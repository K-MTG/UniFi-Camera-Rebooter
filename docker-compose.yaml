services:
  unifi-camera-rebooter:
    build: .
    container_name: unifi-camera-rebooter
    restart: unless-stopped
    volumes:
      - ./config.yaml:/opt/unifi-camera-rebooter/config.yaml:ro
    logging:
      options:
        max-size: "10m"
        max-file: "3"